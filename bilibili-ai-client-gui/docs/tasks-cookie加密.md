# Cookie 加密任务清单

## 模块概述

将扫码登录获取的 Cookie 加密存放，使用 XOR + Base64 方案。

**预计工作量**：1.5 小时

---

## 任务清单

### 1. 加密工具模块

- [x] 1.1 创建 `utils/crypto.py` 文件
- [x] 1.2 实现 `_get_key()` 密钥管理
- [x] 1.3 实现 `_xor_encrypt()` XOR 加密
- [x] 1.4 实现 `encrypt()` 加密函数
- [x] 1.5 实现 `decrypt()` 解密函数
- [x] 1.6 实现 `is_encrypted()` 判断函数
- [x] 1.7 实现 `encrypt_if_needed()` 智能加密
- [x] 1.8 实现 `decrypt_if_needed()` 智能解密
- [x] 1.9 测试加密解密功能

### 2. 配置模块修改

- [x] 2.1 修改 `config.py` 的 `_load()` 方法
- [x] 2.2 修改 `config.py` 的 `save()` 方法
- [x] 2.3 添加 `_migrate_plaintext_cookie()` 迁移方法
- [x] 2.4 测试配置加载保存

### 3. 登录模块修改

- [x] 3.1 修改 `bilibili_login.py` 的 `save_cookie()` 方法
- [x] 3.2 测试登录保存

### 4. GUI 模块修改

- [x] 4.1 修改 `gui/main_window.py` 的 Cookie 显示
- [x] 4.2 隐藏明文 Cookie（显示"已登录（加密存储）"）
- [x] 4.3 测试 GUI 显示

### 5. 数据迁移

- [x] 5.1 实现自动迁移逻辑
- [x] 5.2 测试明文到加密的迁移
- [x] 5.3 测试重复加密防护

### 6. 集成测试

- [x] 6.1 测试完整登录流程
- [x] 6.2 测试配置文件加密
- [x] 6.3 测试 Cookie 文件加密
- [x] 6.4 验证向后兼容性

---

## 完成标准

- [x] Cookie 存储为加密格式（ENC: 前缀）
- [x] 配置文件加载时自动解密
- [x] 保存时自动加密
- [x] 明文数据可自动迁移
- [x] GUI 不显示明文 Cookie
- [x] 密钥文件自动生成

---

## 进度

**完成**：25/25 任务

**状态**：✅ 已完成